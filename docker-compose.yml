name: task-automation

networks:
  tasknet:
    driver: bridge
  # Корп. мониторинг сканирует контейнеры в этой сети через Docker SD.
  # На prod эта сеть создаётся devops'ом (external: true).
  # Локально создать один раз: `docker network create monitoring`
  # (smoke_test / README напоминают).
  monitoring:
    external: true

volumes:
  hermes_state:
  corp_tokens:        # /opt/data/user_tokens/<telegram_id>.json

services:
  # whisper-shim удалён из compose: COMPANY Transcribe уже имеет OpenAI-совм.
  # /api/v1/audio/transcriptions, Hermes ходит туда напрямую (см. hermes/config.yaml).
  # Код shim'а оставлен в whisper_shim/ на случай если upstream API снова
  # расходится и понадобится адаптер.

  metrics-sidecar:
    build:
      context: ./metrics_sidecar
    restart: unless-stopped
    networks: [tasknet, monitoring]
    ports:
      - "8000:8000"
    environment:
      HERMES_LOG_PATH: /hermes_data/logs/agent.log
      HERMES_STATE_DB: /hermes_data/state.db
    volumes:
      # Read-only mount of hermes' /opt/data so we can tail logs + query state.db.
      - ./hermes:/hermes_data:ro
    healthcheck:
      test: ["CMD-SHELL", "python3 -c \"import urllib.request,sys;sys.exit(0 if urllib.request.urlopen('http://127.0.0.1:8000/healthz',timeout=3).status==200 else 1)\""]
      interval: 15s
      timeout: 5s
      retries: 5
      start_period: 10s
    labels:
      - "prometheus_scrape=true"
      - "prometheus_port=8000"
      - "prometheus_path=/metrics"
      - "app_name=hermes-metrics"
      - "app_type=backend"
      - "version=0.2.0"

  hermes:
    image: nousresearch/hermes-agent
    restart: unless-stopped
    networks: [tasknet, monitoring]
    command: ["gateway", "run"]
    ports:
      - "8642:8642"
    environment:
      HERMES_UID: "10000"
      TELEGRAM_BOT_TOKEN: ${TELEGRAM_BOT_TOKEN:?TELEGRAM_BOT_TOKEN must be set in .env}
      TELEGRAM_ALLOWED_USERS: ${TELEGRAM_ALLOWED_USERS:?TELEGRAM_ALLOWED_USERS must be set in .env}
      OPENAI_BASE_URL: ${OPENAI_BASE_URL:?OPENAI_BASE_URL must be set in .env}
      OPENAI_API_KEY: ${OPENAI_API_KEY:?OPENAI_API_KEY must be set in .env}
    volumes:
      - ./hermes:/opt/data
      - ./config:/opt/data/config:ro         # team.yaml для team_directory skill
      - hermes_state:/opt/data/sessions
      - corp_tokens:/opt/data/user_tokens    # PAT-ы пользователей per-user
    labels:
      # Сам Hermes метрик не отдаёт — метрики про него экспортирует
      # metrics-sidecar. Но метим контейнер общими info-метками.
      - "prometheus_scrape=false"
      - "app_name=hermes-gateway"
      - "app_type=backend"
      - "version=0.2.0"
